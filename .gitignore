# dependencies
node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
dist/
/build

# misc
.vscode
.DS_Store
.cache
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env.local
.env.development.local
.env.test.local
.env.production.local

# session transcripts
session-*.md

# vercel
.now
.vercel
.env*.local

# TypeScript incremental build info
*.tsbuildinfo

# pnpm — local-only workspace file auto-generated by pnpm 10+.
# This repo is a single package; Vercel uses pnpm 9 and a committed
# pnpm-workspace.yaml (no `packages:` field) breaks its install.
pnpm-workspace.yaml

# local secrets / scratch files (never commit)
keys.txt
knotctf.txt
*.ctf
